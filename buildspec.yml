version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - echo Build started on 'date'
      - echo Compiling the Node.js code...
      
artifacts:
  files:
    - '**/*'
  discard-paths: yes
